<?php

//add redirect uri rules and shortcode

//code ordering
//code review
//code syntax review
//testing
//final review

// 		$args = array(
// 			'post_type' => 'product',
// 			'posts_per_page' => 5
// 		);

// 		$loop = new WP_Query( $args );
// 		$product_id = null;

// 		if ( $loop->have_posts() ) {
// 			while ( $loop->have_posts() ) : $loop->the_post();
// 				if($product->get_title() == 'yoli2'){
// 					$product_id = $product->get_id();
// 				}
// 			endwhile;
// 		} else {
// 			echo __( 'No products found' );
// 		}//